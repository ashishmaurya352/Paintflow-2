import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Paintflow',
  webDir: 'www',
  // plugins: {
  //   SplashScreen: {
  //     launchShowDuration: 2000, // Time before splash screen is shown (0 means immediately)
  //     launchAutoHide: false, // Set to false so we can manually hide the splash screen
  //     backgroundColor: '#ffffff', // Background color of splash screen
  //     androidSplashResourceName: 'splash', // Android splash screen resource name
  //     showSpinner: true, // Show a spinner during splash screen
  //   },
  // },
};

export default config;
